language: python
python:
  - "3.5"
  - "3.4"
  - "2.7"

install: 
  - pip install BitEx
  - python3 setup.py install

# command to run tests
script: python -m unittest formatters
branches:
  only:
  - master
  - stable